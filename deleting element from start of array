#include <stdio.h>
int main()
{
    int n = 5;
    int arr[5] = {1, 2, 3, 4, 5};
    for (int i = 0; i < n; i++)
    {
        arr[i] = arr[i + 1];
    }
    n--;
    for (int i = 0; i < n; i++)
    {
        printf("%d", arr[i]);
    }

    return 0;
}
