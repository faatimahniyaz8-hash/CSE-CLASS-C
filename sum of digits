#include <stdio.h>
int digitsum(int num){
    int digitsum=0;
    while(num>0){
    int lastdigit =num%10;
    num=num/10;
    digitsum=digitsum+lastdigit;
    }
    return digitsum;
}
int main()
{
  printf("%d",digitsum(123));
    return 0;
}
